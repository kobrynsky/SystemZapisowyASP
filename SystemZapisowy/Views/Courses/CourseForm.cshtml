@model SystemZapisowy.ViewModels.Course.CourseFormViewModel
@{
    ViewBag.Title = "Course form";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Course</h2>

@using (@Html.BeginForm("Save", "Courses"))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Course.CourseName)
        @Html.TextBoxFor(m => m.Course.CourseName, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Course.CourseName)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Course.FieldOfStudyId)
        @Html.DropDownListFor(m => m.Course.FieldOfStudyId, new SelectList(Model.FieldsOfStudy, "FieldOfStudyId", "FieldOfStudyName"), "Select the field of study", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Course.FieldOfStudyId)

    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Course.SemesterId)
        @Html.DropDownListFor(m => m.Course.SemesterId, new SelectList(Model.Semesters, "SemesterId", "SemesterName"), "Select the semester", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Course.SemesterId)
    </div>
    @Html.HiddenFor(m => m.Course.CourseId)
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
